<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.companyname.ukrainecitiesapp">

    <!-- Дозволи для доступу до інтернету та геолокації -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="UkraineCitiesApp"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        
        <!-- Налаштування безпеки мережі -->
        <meta-data
            android:name="android.security.network-security-config"
            android:resource="@xml/network_security_config" />

        <!-- Google Maps API ключ -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="*********" />

        <!-- Додаткові бібліотеки (якщо потрібно) -->
        <uses-library android:name="org.apache.http.legacy" android:required="false" />

    </application>
</manifest>
